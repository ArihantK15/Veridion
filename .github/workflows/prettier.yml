name: Prettier

on:
  pull_request:
  push:
    branches:
      - master
      - main

jobs:
  prettier:
    name: Prettier placeholder
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Prettier placeholder
        run: 'echo "TODO: enable Prettier check once dependencies are finalized"'

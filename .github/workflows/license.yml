name: License Check

on:
  pull_request:
  push:
    branches:
      - master
      - main

jobs:
  license:
    name: License check placeholder
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: License check placeholder
        run: 'echo "TODO: enable license checks once dependency policy is finalized"'

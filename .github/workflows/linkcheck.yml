name: Link Check

on:
  pull_request:
  push:
    branches:
      - master
      - main

jobs:
  linkcheck:
    name: Link check placeholder
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Link check placeholder
        run: 'echo "TODO: enable link checking once documentation policy is finalized"'
